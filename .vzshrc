echo "Setting up: aliases" 

dck-ssh() {
	sudo docker run -i -t $1 /bin/bash
}

alias dck='sudo docker'
alias dck-build='sudo docker build .'
alias dck-ps='sudo docker ps'
alias dck-tag='sudo docker tag'
alias dck-ssh='sudo docker run -i'

alias ll='ls -lG'
alias rvm='~/scripts/rvm.tcsh'
alias mk='make -f makefile.mk'
alias sa='make server'
alias ss='make server'
alias so='make reverse'